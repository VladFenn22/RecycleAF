import { createTheme } from '@mui/material/styles';
export const appTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
          main: '#216725',
        },
        secondary: {
          main: '#004d40',
        },
        background: {
          paper: '#8bc34a',
          default: '#fdfffd',
        },
      },
});
